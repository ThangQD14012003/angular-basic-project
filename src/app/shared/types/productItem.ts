export type ProductItems = {
    id: number; 
    name: string; 
    price: number; 
    image: string; 
    description: string
}

export type BlogItem = {
    id?: number;
    title?: string;
    body?:string; 
    author?: string
}